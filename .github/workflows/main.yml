name: main

on:
  pull_request:

jobs:
  handle:
    runs-on: ubuntu-latest
    steps:
      - name: handle
        run: |
          echo 'Label: ${{ github.event.pull_request.labels.perm }}'
          echo 'contains ${{ contains('tomkre', 'tom') }}'
          
