name: Main

on:
  push:
    branches: [main]

jobs:
  handle:
    runs-on: ubuntu-latest
    steps:
      - name: Print token
        env:
          WF_TOKEN: ${{ secrets.WF }}
        run: |
          echo 'WF token: ${{ env.WF_TOKEN }}'

