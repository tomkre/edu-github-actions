name: Main

on:
  push:
    branches:
      - main
      - 'rel/**'

jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: echo 'Running job one in main workflow'
            

