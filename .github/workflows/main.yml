name: Main

on:
  push:
    branches: [main]

jobs:
  handle:
    runs-on: ubuntu-latest
    steps:
      - name: Print event name
        run: |
          echo 'Event name: ${{ github.event.name }}'

