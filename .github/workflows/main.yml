name: main

on:
  push:
  pull_request:

jobs:
  job_one:
    runs-on: ubuntu-latest
    steps:
      - name: step_one
        run: |
          echo 'Running step_one'
          exit 0
  job_two:
    runs-on: ubuntu-latest
    steps:
      - name: step_one
        run: |
          echo 'Running step_one in feature/2'
          exit 1
          
