run-name: Master workflow run by @${{ github.actor }}

on:
  push:
    branches: [main]

env:
  AUTHOR: ${{ vars.AUTHOR }}

jobs:
  print_author:
    runs-on: ubuntu-latest
    steps:
      - name: Print Author
        run: |
          echo ${{ env.AUTHOR }}